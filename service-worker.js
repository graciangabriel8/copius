/* Copius service worker — generated by tools/build-sw.py. Do not edit by hand. */
const VERSION = 'v35';
const CACHE = 'copius-' + VERSION;
const ASSETS = [
  './',
  './index.html',
  'icons/icon-192.png',
  'icons/icon-512.png',
  'icons/apple-touch-icon.png',
  'manifest.json',
  'css/style.css?v=35',
  'js/app.js?v=35',
  'js/data-chefs.js?v=35',
  'js/data-condiments.js?v=35',
  'js/data-cuts.js?v=35',
  'js/data-dairy.js?v=35',
  'js/data-fats.js?v=35',
  'js/data-fruits.js?v=35',
  'js/data-grains.js?v=35',
  'js/data-herbs.js?v=35',
  'js/data-legumes.js?v=35',
  'js/data-meat.js?v=35',
  'js/data-mushrooms.js?v=35',
  'js/data-nuts.js?v=35',
  'js/data-roe.js?v=35',
  'js/data-seafood.js?v=35',
  'js/data-seaweed.js?v=35',
  'js/data-shellfish.js?v=35',
  'js/data-spices.js?v=35',
  'js/data-sweet.js?v=35',
  'js/data-trees.js?v=35',
  'js/data-vegetables.js?v=35',
  'js/i18n.js?v=35',
  'js/photos.js?v=35',
  'js/trios-gastronomy.js?v=35',
  'js/trios-premium.js?v=35',
  'js/trios.js?v=35',
];

self.addEventListener('install', function (e) {
  e.waitUntil(caches.open(CACHE).then(function (c) { return c.addAll(ASSETS); }).then(function () {
    return self.skipWaiting();
  }));
});

self.addEventListener('activate', function (e) {
  e.waitUntil(caches.keys().then(function (keys) {
    return Promise.all(keys.filter(function (k) {
      return k.indexOf('copius-') === 0 && k !== CACHE;
    }).map(function (k) { return caches.delete(k); }));
  }).then(function () { return self.clients.claim(); }));
});

self.addEventListener('fetch', function (e) {
  var req = e.request;
  if (req.method !== 'GET') return;
  if (new URL(req.url).origin !== self.location.origin) return;

  // The page itself: network first, so a new deploy is picked up. Cache is the offline fallback.
  if (req.mode === 'navigate') {
    e.respondWith(
      fetch(req).then(function (res) {
        var copy = res.clone();
        caches.open(CACHE).then(function (c) { c.put('./index.html', copy); });
        return res;
      }).catch(function () {
        return caches.match('./index.html');
      })
    );
    return;
  }

  // Everything else is versioned by ?v=, so cache-first is safe and instant.
  e.respondWith(
    caches.match(req).then(function (hit) {
      return hit || fetch(req).then(function (res) {
        if (res && res.status === 200) {
          var copy = res.clone();
          caches.open(CACHE).then(function (c) { c.put(req, copy); });
        }
        return res;
      });
    })
  );
});
