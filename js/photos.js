// Optional real photos. Drop a square JPG named <id>.jpg into img/
// (e.g. img/tomato.jpg) and add its id to this list — the site will use
// the photo instead of the engraved illustration, on cards and detail pages.
window.PHOTOS = [];
